const DASHBOARD = '/'

const LOGIN = '/login'

const REGISTER = '/register'

const RESET_PASSWORD = '/reset-password'

const ADDDRINK = '/add_drink/'

const HISTORY = '/history'

export default {
  HISTORY,
  LOGIN,
  REGISTER,
  RESET_PASSWORD,
  DASHBOARD,
  ADDDRINK,
}
